# 🎯 通配符填空练习

## 练习目标

这才是学习通配符的**正确方式**：

- ❌ 不是让你实现方法体（遍历、添加等）
- ✅ 而是让你**判断和填写通配符**

## 如何练习

### 第1步：打开文件

```
WildcardFillInBlanks.java
```

### 第2步：分析每个方法

每个方法都有详细的分析提示：

```java
/**
 * 🎯 TODO 1: 填写通配符
 *
 * 需求：这个方法需要打印动物列表
 * 分析：
 * - 需要从列表中**读取**数据吗？ 是/否
 * - 需要往列表中**写入**数据吗？ 是/否
 *
 * 问题：应该填什么？
 * A. List<Animal>
 * B. List<? extends Animal>
 * C. List<? super Animal>
 * D. List<T>
 */
private static void printAnimals(List<___填写通配符___> animals) {
    // ...
}
```

### 第3步：填写通配符

替换 `___填写通配符___` 为正确的通配符：

```java
// 例如：
List<? extends Animal>
List<? super Dog>
List<T>
```

### 第4步：运行验证

填写完成后，运行 `main()` 方法验证是否正确。

如果通过，会看到：
```
🎉 所有测试通过！
```

---

## 练习题目

### 练习1：打印动物列表 ⭐
- 需求：打印动物列表
- 操作：只读取
- 思考：用什么通配符？

### 练习2：添加狗到列表 ⭐
- 需求：把Dog添加到列表
- 操作：只写入
- 思考：用什么通配符？

### 练习3：复制列表 ⭐⭐
- 需求：从src复制到dest
- 操作：src读取，dest写入
- 思考：两个参数分别用什么？

### 练习4：找最大值 ⭐⭐
- 需求：找列表中最大值
- 操作：只读取
- 思考：用什么通配符？

### 练习5：批量添加 ⭐⭐
- 需求：把src的元素加到dest
- 操作：src读取，dest写入
- 思考：两个参数分别用什么？

### 练习6：交换元素 ⭐⭐⭐
- 需求：交换列表中两个元素
- 操作：**既读又写**
- 思考：这种情况用什么？

---

## 判断技巧

### 口诀

```
只读用 extends（生产者）
只写用 super（消费者）
既读又写用 T
```

### 判断流程

```
第1步：这个方法需要从列表读数据吗？
  ↓ 是
第2步：这个方法需要往列表写数据吗？
  ↓ 否
答案：用 ? extends T

第1步：这个方法需要从列表读数据吗？
  ↓ 否
第2步：这个方法需要往列表写数据吗？
  ↓ 是
答案：用 ? super T

第1步：这个方法需要从列表读数据吗？
  ↓ 是
第2步：这个方法需要往列表写数据吗？
  ↓ 是
答案：用 T（普通泛型）
```

---

## 参考答案

完成后可以对照 `answer/WildcardFillInBlanksAnswer.java`

但**先自己思考，再看答案**！

---

**准备好了吗？开始填空吧！** 🚀
